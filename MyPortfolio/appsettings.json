{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "${ALLOWED_ORIGINS}"
    ]
  },
  "ConnectionStrings": {
    "DataDBConnection": "Host=${DB_HOST};Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}",
    "StockPriceWatcherURL": "${STOCK_PRICE_WATCHER_URL}"
  }
}